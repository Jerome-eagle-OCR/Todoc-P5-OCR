<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.MainActivity">

    <item
        android:id="@+id/action_filter"
        android:icon="@drawable/ic_filter_list"
        android:title="@string/label_action_filter"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/sort_by_project"
                android:title="@string/sort_by_project"
                app:showAsAction="never|withText" />
            <item
                android:id="@+id/sort_oldest_first"
                android:title="@string/sort_oldest_first"
                app:showAsAction="never|withText" />
            <item
                android:id="@+id/sort_recent_first"
                android:title="@string/sort_recent_first"
                app:showAsAction="never|withText" />
        </menu>
    </item>

</menu>